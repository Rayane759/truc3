apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}
  namespace: ${{ values.namespace }}
spec:
  selector:
    app: ${{ values.appName }}
  ports:
    - port: 80
      targetPort: ${{ values.service.port }}
  type: ClusterIP