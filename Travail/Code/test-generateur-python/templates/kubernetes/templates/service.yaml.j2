apiVersion: v1
kind: Service
metadata:
  name: {{ appName }}
  namespace: {{ namespace }}
spec:
  selector:
    app: {{ appName }}
  ports:
    - port: 80
      targetPort: {{ servicePort }}
  type: ClusterIP